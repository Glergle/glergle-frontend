const { colors } = require('tailwindcss/defaultTheme')

module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          default: '#4FD1C5',
        },
        secondary: {
          default: '#68D391',
        },
      }
    }
  }
}